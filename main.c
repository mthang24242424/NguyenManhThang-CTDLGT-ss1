#include <stdio.h>

int main() {
    for (int i = 0; i < n; i ++) {
        printf("hello \n");
    }
    return 0;
}
// đoạn mã sử dụng 1 vòng lặp for in ra từ hello n lần, độ phức tạp về thời gian là O(n)